name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: openjdk8
      run: docker build . --file java/Dockerfile --tag openjdk8:$(date +%s)
